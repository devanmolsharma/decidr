[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "decidr"
version = "0.8.0"
description = "Typed decisions from an LLM in one forward pass -- OpenAI, Ollama, or any OpenAI-compatible endpoint"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
keywords = ["llm", "openai", "ollama", "classification", "routing", "logprobs", "decisions"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = ["openai>=1.0"]

[project.optional-dependencies]
dev = ["pytest>=7"]

[project.urls]
Homepage = "https://github.com/devanmolsharma/decidr"
Issues = "https://github.com/devanmolsharma/decidr/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/decidr"]

[tool.pytest.ini_options]
testpaths = ["tests"]
